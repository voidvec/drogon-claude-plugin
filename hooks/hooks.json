{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import json,os; r=os.environ.get('CLAUDE_PLUGIN_ROOT',''); p=os.path.join(r,'CLAUDE.md'); print(json.dumps({'hookSpecificOutput':{'hookEventName':'SessionStart','additionalContext':open(p,encoding='utf-8').read()}})) if r and os.path.exists(p) else None\"",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/hooks/posttooluse.py\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
